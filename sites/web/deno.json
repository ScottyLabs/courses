{
  "nodeModulesDir": "auto",
  "tasks": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "generate-api": "openapi-typescript http://localhost:3002/api/openapi.json -o src/lib/api.d.ts"
  }
}
