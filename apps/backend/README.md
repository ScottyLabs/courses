# CMU Courses Backend

TODO:

- [ ] logging
    - use log instead of println
    - log internal server errors before returning generic 500 responses
- [ ] tests directory
- [ ] check if no migrations is applied when RUN_MIGRATIONS_ON_STARTUP is set
- [ ] maybe use envy as a typed config layer
